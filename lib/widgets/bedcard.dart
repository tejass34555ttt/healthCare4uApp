import 'package:flutter/material.dart';

class Bedcard extends StatelessWidget {
  final String title;
  final int? count;

  const Bedcard({super.key, required this.title, this.count});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
